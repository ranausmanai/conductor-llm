# Core dependencies (no external LLM packages required for base functionality)
# The package works without any external dependencies for dry-run/evaluation mode

# Optional: For actual LLM execution
openai>=1.0.0
anthropic>=0.18.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0
